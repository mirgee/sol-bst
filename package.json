{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.15"
  },
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat ignition deploy ./ignition/modules/BinarySearchTree.js",
    "test": "hardhat test"
  }
}
